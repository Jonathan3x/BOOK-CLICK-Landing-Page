function searchclick() {
    
    let userbook = document.querySelector(".inputbar");
    let bookvalue = userbook.value.trim();
    
    if (bookvalue === "")
    alert ("Ingresa el nombre de un libro.")
    else {
        alert("El sitio está en construcción, vuelve más tarde.");
        return;
    }

}
